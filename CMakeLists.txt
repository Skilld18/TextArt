cmake_minimum_required(VERSION 2.8)
project(draw)
find_package(OpenCV REQUIRED )
set (CMAKE_CXX_STANDARD 11)
add_executable(draw main.cpp image.cpp utils.cpp utils.h image.h)
target_link_libraries(draw ${OpenCV_LIBS} )
